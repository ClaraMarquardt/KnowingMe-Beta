
# directory
cd -- "$(dirname -- "$BASH_SOURCE")"

# check that in correct directory
current_path=$(pwd)
echo $current_paths

# load settings
source codebase/setup_manual/exec_setting.sh

# start-app
${python_custom} codebase/KnowingMe.py

$SHELL 